name: Full CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  backend-ci:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json

    - name: Install dependencies
      run: |
        cd backend
        npm ci

    - name: Lint
      run: |
        cd backend
        npm run lint

    - name: Build
      run: |
        cd backend
        npm run build

    - name: Run unit tests
      run: |
        cd backend
        npm run test

    - name: Run e2e tests
      run: |
        cd backend
        npm run test:e2e

  frontend-ci:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'pnpm'
        cache-dependency-path: frontend/pnpm-lock.yaml

    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 10.30.1

    - name: Install dependencies
      run: |
        cd frontend
        pnpm install --frozen-lockfile

    - name: Lint
      run: |
        cd frontend
        pnpm run lint

    - name: Build
      run: |
        cd frontend
        pnpm run build

    - name: Run unit tests
      run: |
        cd frontend
        pnpm run test

    - name: Run Cypress tests
      uses: cypress-io/github-action@v6
      with:
        working-directory: frontend
        build: pnpm run build
        start: pnpm run start
        wait-on: 'http://localhost:3000'
        wait-on-timeout: 120
        browser: chrome
        record: false

  integration-tests:
    needs: [backend-ci, frontend-ci]
    runs-on: ubuntu-latest
    
    services:
      backend:
        image: node:20
        options: --health-cmd="curl -f http://localhost:3000/health || exit 1" --health-interval=30s --health-timeout=10s --health-retries=3

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json

    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 10.30.1

    - name: Install backend dependencies
      run: |
        cd backend
        npm ci

    - name: Install frontend dependencies
      run: |
        cd frontend
        pnpm install --frozen-lockfile

    - name: Build backend
      run: |
        cd backend
        npm run build

    - name: Build frontend
      run: |
        cd frontend
        pnpm run build

    - name: Start backend
      run: |
        cd backend
        npm run start:prod &
        sleep 10

    - name: Start frontend
      run: |
        cd frontend
        pnpm run start &
        sleep 10

    - name: Run integration tests
      run: |
        cd frontend
        pnpm run cypress:run

    - name: Upload integration test artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: integration-test-results
        path: |
          frontend/cypress/screenshots
          frontend/cypress/videos

  deploy-ready:
    needs: [integration-tests]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deployment ready notification
      run: |
        echo "🚀 All tests passed! Application is ready for deployment."
        echo "Backend and Frontend CI completed successfully."
        echo "Integration tests passed."