name: Deploy Backend

on:
  workflow_run:
    workflows: ["Build Backend Docker Image"]
    types: [completed]

permissions:
  contents: read

jobs:
  deploy:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Render deploy disabled (not used)
        run: echo "Render deploy step skipped"