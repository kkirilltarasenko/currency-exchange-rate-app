# 📚 Глоссарий - Приложение курсов валют

## 💱 Валютные и финансовые термины

### **Базовая валюта**
Первая валюта в валютной паре, используемая как точка отсчета для расчетов обменного курса (например, в USD/EUR, USD является базовой валютой).

### **Валютная пара**
Две валюты, сравниваемые в обменном курсе (например, USD/EUR, GBP/JPY).

### **Валютный код**
Трехбуквенный идентификатор валют, следующий стандарту ISO 4217 (например, USD для доллара США, EUR для евро).

### **Исторические данные**
Архивная информация о курсах валют за прошлые периоды, используемая для анализа трендов и построения графиков динамики.

### **Конвертер валют**
Функция, позволяющая пользователям рассчитывать эквивалентную сумму одной валюты в другой валюте, используя текущие обменные курсы.

### **Котировочная валюта**
Вторая валюта в валютной паре, показывающая, сколько единиц этой валюты требуется для покупки одной единицы базовой валюты.

### **Курс обмена**
Стоимость одной валюты, выраженная в единицах другой валюты, показывающая, сколько котировочной валюты необходимо для покупки одной единицы базовой валюты.

---

## 🔧 Компоненты системы

### **Chart (График)**
Компонент системы, отвечающий за генерацию и отображение графиков изменения курсов валют. Использует данные из [`ExchangeRate`](app/components/ExchangeRate.tsx) для создания визуализаций.

### **Currency (Валюта)**
Основной класс, представляющий валюту в системе. Содержит:
- `code` - код валюты (например, USD, EUR)
- `name` - полное название валюты
- `symbol` - символ валюты (например, $, €)

### **CurrencyConverter (Конвертер валют)**
Компонент, предоставляющий функциональность конвертации валют. Основной метод:
- `convert(amount, fromCurrency, toCurrency)` - конвертирует сумму из одной валюты в другую

### **DataProvider (Поставщик данных)**
Класс, отвечающий за получение данных от внешних API. Содержит:
- `providerName` - название провайдера данных
- `apiUrl` - URL API для получения данных
- `fetchRates()` - метод получения курсов валют

### **ExchangeRate (Обменный курс)**
Класс, представляющий курс обмена между валютами. Содержит:
- `rateValue` - значение курса обмена
- `date` - дата и время курса
- Связан с базовой и целевой валютами

### **ExchangeRateService (Сервис обменных курсов)**
Основной сервисный класс для работы с курсами валют. Предоставляет методы:
- `getCurrentRates()` - получение текущих курсов
- `getHistoricalRates()` - получение исторических данных
- Использует [`DataProvider`](app/services/DataProvider.ts) для получения данных

### **User (Пользователь)**
Класс, представляющий пользователя системы. Содержит:
- `userId` - уникальный идентификатор пользователя
- `selectedCurrencies` - выбранные пользователем валюты
- `preferences` - пользовательские настройки
- `selectCurrency()` - метод выбора валюты
- `viewRates()` - метод просмотра курсов

---